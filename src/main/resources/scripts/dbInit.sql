CREATE TABLE moduleFromSqlScript
(
    ID int,
    MODULEID bigint,
    NAME varchar(255),
    MODULEVERSION varchar(255),
    VERSIONSTATE varchar(255)
);

CREATE TABLE metricFromSqlScript
(
  ID int,
  MODULEID bigint,
  NAME varchar(255),
  MODULEVERSION varchar(255),
  VERSIONSTATE varchar(255)
);

